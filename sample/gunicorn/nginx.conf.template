pid {conf_dir}/nginx.pid;

worker_processes 4;

events {}

http {
    upstream gunicorn {
        server 127.0.0.1:{gunicorn};
    }
     
    server {
        listen *:8080;
        location / {
            proxy_pass  http://gunicorn;
        }
    }

}
